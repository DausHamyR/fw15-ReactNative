import React from 'react';
import SelectDropdown from 'react-native-select-dropdown';

const SelectDrowdown = ({...rest}) => {
  return <SelectDropdown {...rest} />;
};

export default SelectDrowdown;
